/*import {productList} from "../models/objects"
import {Products} from "../models/Products"
function addToCart ({category} :Products ){
    let cartSymbol = document.getElementsByClassName("fa-cart-plus");
    
    
    for(let i = 0; i<cartSymbol.length; i ++){
        let addButton = cartSymbol[i] as HTMLElement;
        addButton.addEventListener("click", ()=>{

        })
    }

}*/
